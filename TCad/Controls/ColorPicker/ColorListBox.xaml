<UserControl x:Class="TCad.Controls.ColorListBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TCad.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>

            <DrawingBrush  x:Key = "CheckerBrush2"
                           Viewport="0,0,8,8"
                           ViewportUnits="Absolute"
                           TileMode="Tile">
                
                <DrawingBrush.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="White">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="0,0,8,8" />
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>

                        <GeometryDrawing Brush="DarkGray">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="0,0,4,4" />
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>

                        <GeometryDrawing Brush="DarkGray">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="4,4,4,4" />
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingBrush.Drawing>
            </DrawingBrush>


        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ListBox Name="ColorList"
            ScrollViewer.VerticalScrollBarVisibility="Visible"
            Grid.Row="0"  d:ItemsSource="{d:SampleData ItemCount=5}"
            SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Grid>
                            <Rectangle Fill="{StaticResource CheckerBrush2}"
                                Width="24"
                                Height="16"
                                Margin="2"
                                />
                            <Rectangle
                                Stroke="Black" StrokeThickness="1"
                                Fill="{Binding Brush}"
                                Width="24"
                                Height="16"
                                Margin="2"
                                />
                        </Grid>

                        <TextBlock Text="{Binding Name}"
                                       Margin="2"
                                       />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
